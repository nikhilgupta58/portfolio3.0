import { Flex, Text } from "@chakra-ui/react";
import { useState } from "react";

function App() {
  const [count, setCount] = useState(0);
  return (
    <Flex>
      <Text>kenfke</Text>
    </Flex>
  );
}

export default App;
